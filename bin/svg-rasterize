#!/usr/bin/env bash
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=../lib/svg.sh
source "${HERE}/../lib/svg.sh"
# Usage: svg-rasterize <in.svg> <out.png> [size]
svg_rasterize "$@"
